{
  "name": "travel-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/seo-build.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "vite": "^7.3.1"
  }
}
